{
  "name": "atomcode2api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "claim-pro": "bun run src/auto-claim.ts",
    "monitor": "bun run src/monitor.ts",
    "capture": "bun run src/capture.ts"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "pino-roll": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}
